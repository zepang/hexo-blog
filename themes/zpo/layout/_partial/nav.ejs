<nav id="page-nav-wrapper">
  <ul class="nav-list">
    <li class="nav-item">
      <a href="" class="logo">
        <h1>zpo.</h1>
      </a>
    </li>
    <% for (const menu in menus) { %>
      <% if (menus.hasOwnProperty(menu)) { %>
        <% const current = menus[menu] %>
        <li class="nav-item">
          <a href="" class=<%= current.en %>><%= current.en %></a>
        </li>
      <% } %>
    <% } %>
  </ul>
</nav>